apiVersion: apps/v1
kind: Deployment
metadata:
  name: ex5 
  labels:
    app: ex5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ex5
  template:
    metadata:
      labels:
        app: ex5
    spec:
      containers:
        - name: ex5-container
          image: daryn0201/ex5:latest # 👈 заменишь на свой dockerhub
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10